"""Demo-mode simulators for offline calibration baselines.

These modules generate synthetic data that matches a known reference
dataset so the UI / report-generation paths can be exercised without
touching live PSU / scope / DUT hardware.
"""
