[pytest]
asyncio_mode = auto
testpaths = tests
python_files = test_*.py
# SQLModel ORM classes named ``TestRun`` / ``TestStatus`` etc. are domain
# models, not pytest collections — suppress the noisy collection warning.
filterwarnings =
    ignore::pytest.PytestCollectionWarning
