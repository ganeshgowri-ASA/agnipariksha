fastapi>=0.111.0
uvicorn[standard]>=0.29.0
pydantic>=2.7.0
pydantic-settings>=2.2.1
websockets>=12.0
python-dotenv>=1.0.0
httpx>=0.27.0
loguru>=0.7.2

# Hardware I/O
pyserial>=3.5
pyvisa-py>=0.7.2

# Device registry manifests
PyYAML>=6.0

# Report generation (server-side)
python-docx>=1.1.2
reportlab>=4.2.0
matplotlib>=3.9.0

# Persistence (V2-S2 — SQLModel + Alembic; SQLite default, Postgres optional)
sqlmodel>=0.0.16
SQLAlchemy>=2.0.30
alembic>=1.13.1

# DB connectors (feat/db-connectors): Fernet for at-rest secret encryption,
# keyring for OS-native key storage. Driver wheels are optional / lazy.
cryptography>=42.0.0
keyring>=24.0.0

# Tests
pytest>=8.0.0
pytest-asyncio>=0.23.0
