fastapi>=0.111.0
uvicorn[standard]>=0.29.0
pydantic>=2.7.0
pydantic-settings>=2.2.1
websockets>=12.0
python-dotenv>=1.0.0
httpx>=0.27.0
loguru>=0.7.2

# Hardware I/O
pyserial>=3.5
pyvisa-py>=0.7.2

# Report generation (server-side)
python-docx>=1.1.2
reportlab>=4.2.0
matplotlib>=3.9.0

# Auth
PyJWT>=2.8.0

# Web Push (VAPID) is optional — install separately for production:
#   pip install 'pywebpush>=2.0.0'
# Kept out of the base requirements because its native deps (http-ece /
# cryptography) lack wheels on some hosts and break `pip install -r`.

# QR generation (used by reportlab + label fallbacks)
qrcode[pil]>=7.4.2

# Tests
pytest>=8.0.0
pytest-asyncio>=0.23.0
