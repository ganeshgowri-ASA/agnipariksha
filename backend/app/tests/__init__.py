"""IEC test orchestrators (S1..S6).

Each module here exposes a high-level ``run(...)`` orchestrator plus the
simulator + analysis helpers used by the FastAPI control plane and the
report generator.
"""
